import { Selector } from "testcafe";

fixture("AnatomiesSideBarConnected").page(
  "http://localhost:6060/#!/AnatomiesSideBarConnected",
);

test("Stub", async (t) => {
  const example = Selector("div");
});
